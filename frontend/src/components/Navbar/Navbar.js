import { NavLink } from "react-router-dom";
import "./Navbar.css"; // Ensure you have a separate CSS file for styles

const Navbar = () => {
  return (
    <nav className="navbar">
      <h2 className="navbar-title">Transaction Dashboard</h2>
    </nav>
  );
};

export default Navbar;
